language: python
python:
  - "3.5"
install:
  - "pip install -r requirements.txt"
  - "pip install coveralls"
  - "pip install -e ."
before_script:
  - flake8
script:
  - coverage run -m pytest
after_success:
  - coveralls
notifications:
  email: false